spring:
  batch:
    initialize-schema: always

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/batch_io
    username: root
    password: chillo
    #initialization-mode: always
    continueOnError: false

  jpa:
    hibernate:
      ddl-auto: create
